package today.creame.web.m2net.domain;

import org.springframework.data.jpa.repository.JpaRepository;

public interface M2netRecepitHistroyJpaRepository extends JpaRepository<M2netNoticeHistory, Long> {
}
